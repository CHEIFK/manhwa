{
  "name": "Manwa Manager",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "forwardPorts": [
    8765
  ],
  "portsAttributes": {
    "8765": {
      "label": "Manwa Manager",
      "protocol": "http",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "if [ -f manwa_manager/requirements.txt ]; then pip install -r manwa_manager/requirements.txt; else pip install -r requirements.txt; fi",
  "postStartCommand": "if [ -d manwa_manager ]; then cd manwa_manager; fi && nohup python3 app.py > /tmp/manwa-manager.log 2>&1 &"
}
